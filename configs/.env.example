# Yufa-MT environment configuration.
# Copy to ".env" at the repo root and fill in values. The app loads ".env"
# automatically via godotenv. Anything set in the real shell environment
# takes precedence over ".env".

# --- Admin ---
# If unset, a random password is generated on startup and written to
# data/pwd.txt. For production, set this explicitly.
ADMIN_PASSWORD=

# --- Discord bot ---
# Bot token from the Discord developer portal.
DISCORD_BOT_TOKEN=
# Comma-separated channel IDs the bot should listen in.
DISCORD_CHANNEL_IDS=

# --- Gemini (Google Generative AI) ---
# API key used to parse free-text trade messages into structured items.
GEMINI_API_KEY=

# --- Chat capture (optional) ---
# Network device to sniff for in-game chat packets (e.g. "eth0"). Leave
# unset to disable chat capture.
CHAT_CAPTURE_DEVICE=
# TCP port of the game server to filter packets on.
CHAT_CAPTURE_PORT=

# --- In-game player count: /who injection (optional, default OFF) ---
# When set (1/true/yes), the players-ingame job injects a raw CZ_REQ_USER_COUNT
# (0x00c1, the client's /who) as its preferred count source. Leave OFF unless
# this server's packetver accepts /who: on servers that don't register 0x00c1,
# rAthena disconnects the session on the injected packet, dropping the live game
# client (and the chat capture) every time the job runs. The @users atcommand
# fallback runs regardless.
ZONE_PROXY_WHO=
